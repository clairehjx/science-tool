You are extracting MULTIPLE-CHOICE questions from a Singapore Primary School Science exam paper.

CRITICAL TASK: Extract ONLY multiple-choice questions from BOOKLET A — NOT open-ended.

School: {SCHOOL}
Year: {YEAR}
Level: {LEVEL}

APPROVED TOPIC NAMES — use these exactly, pick the closest match:
{TOPICS}

WHAT TO EXTRACT
- Booklet A only. Booklet A is the multiple-choice section, typically Q1–Q28 or Q1–Q30.
- Stop when you reach Booklet B (open-ended). Do not include open-ended questions.
- Skip the cover page, instructions page, and any answer-grid / OMR sheet pages.
- Each MCQ has a stem and four options labelled (1)/(2)/(3)/(4) OR A/B/C/D. Normalise to A/B/C/D in the output.

WHERE TO FIND THE CORRECT ANSWER
- Look for an answer key at the back of the booklet (sometimes printed at the very end of the PDF, sometimes on a separate marker-sheet page).
- If the answer key uses 1/2/3/4, convert to A/B/C/D (1→A, 2→B, 3→C, 4→D).
- If the answer key for a particular question is missing or unreadable, SKIP that question entirely.

OUTPUT: a JSON array. For each MCQ:
{
  "question_kind": "mcq",
  "parent_question_id": "1",
  "question_text": "full verbatim stem, exactly as it appears (including NOT/EXCEPT in caps if present)",
  "options": {
    "A": "option A text, verbatim",
    "B": "option B text, verbatim",
    "C": "option C text, verbatim",
    "D": "option D text, verbatim"
  },
  "correct_option": "B",
  "diagrams_and_tables": [
    {"type": "diagram|table|graph|circuit|image|flowchart", "description": "..."}
  ],
  "marks": 2,
  "topic": "exact topic name from APPROVED TOPIC NAMES above",
  "page_number": 3,
  "bbox": {
    "top_y_norm": 120,
    "bottom_y_norm": 480
  }
}

VERBATIM STEM RULE — STRICT
- The `question_text` MUST be the full stem exactly as printed. We will text-search the PDF on this string later to crop a screenshot, so paraphrasing or summarising will break the cropping step.
- Preserve original capitalisation (e.g. "NOT", "EXCEPT", "MOST", "LEAST" if uppercased).
- Preserve trailing punctuation. Do not add or remove punctuation.
- If the stem refers to a diagram/table that appears on a separate line ("The diagram below shows..."), include that wording in `question_text`.

OPTION TEXT RULE
- Options must be verbatim too — same capitalisation, same wording.
- If options use "(1)/(2)/(3)/(4)" labelling in the source, still output them under keys A/B/C/D in the same order.

PAGE NUMBER
- `page_number` is the 1-indexed PDF page where the question stem starts (the page you extracted it from).
- If the stem starts on page 3 but options continue onto page 4, still use 3.

BBOX RULES — used downstream to crop the question
- `bbox.top_y_norm` and `bbox.bottom_y_norm` are integers 0..1000, normalised to
  the height of `page_number` (0 = top of page, 1000 = bottom of page).
- The bbox must tightly enclose the ENTIRE question on `page_number`: stem +
  any associated diagram or table + all four options. Do NOT include the next
  question or substantial blank margins.
- If the question spans onto the next page, set `bottom_y_norm: 1000` — the
  cropping step will handle stitching.
- Both fields are REQUIRED — never null. If you cannot locate a question
  visually on the page, omit it from the output.

DIAGRAM DESCRIPTION RULES (for `diagrams_and_tables`)
- Describe WHAT the diagram shows and all labeled parts with letters/numbers, measurements, arrows, scales.
- For tables: list column headers and row labels exactly.
- For graphs: both axes (labels and ranges), key data points, legend.
- For circuits: components, series/parallel arrangement, switch states.
- For life cycles / flowcharts: stages in order with their relationships.

MARKS
- Most P4/P5 MCQs are 2 marks each; use the value printed alongside the question. If unclear, use 2.

GENERAL RULES
- Maintain ascending question order: Q1, Q2, …
- Skip parent-only entries (MCQ doesn't have these).
- Output ONLY the JSON array. No markdown fences, no commentary before or after.
